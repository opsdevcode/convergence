# Diagram source

Reserved for editable exports (SVG, draw.io, and similar) if Mermaid is not
enough.

v0.1 uses only Mermaid in the sibling Markdown files. Do not add generated
assets until there is a reason to pin a rendered form.
